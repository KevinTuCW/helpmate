services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: helpmate
      POSTGRES_PASSWORD: helpmate
      POSTGRES_DB: helpmate
    ports: ["5432:5432"]
    volumes: ["helpmate_pg:/var/lib/postgresql/data"]
volumes:
  helpmate_pg:
